<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="9dp"
    android:layout_margin="5dp">

    <de.hdodenhof.circleimageview.CircleImageView
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/profile_image"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:src="@drawable/ic_user_account"
        app:civ_border_width="2dp"
        app:civ_border_color="@color/titles_color">
    </de.hdodenhof.circleimageview.CircleImageView>

    <TextView
        android:id="@+id/show_text_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@+id/profile_image"
        android:layout_marginStart="6dp"
        android:text="testing purposes"
        android:textColor="@color/text_color_Sub_Title"
        android:textStyle="bold"
        android:textSize="18sp"
        android:padding="8dp"
        android:background="@drawable/ic_background_message_left"/>

    <ImageView
        android:id="@+id/left_image_view"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_toEndOf="@+id/profile_image"
        android:layout_marginStart="6dp"
        android:visibility="gone"
        tools:ignore="ContentDescription" />

    <TextView
        android:id="@+id/text_seen"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/titles_color"
        android:visibility="gone"
        android:layout_below="@+id/show_text_message"/>

</RelativeLayout>